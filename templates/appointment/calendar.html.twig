{% extends 'base.html.twig' %}

{% block body %}
    <h1>Calendar</h1>

    <div class="appointment-form">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Save Appointment</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
